<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" android:title="Ala">

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="example_checkbox"
        android:summary="@string/pref_description_social_recommendations"
        android:title="@string/pref_title_social_recommendations" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_add_friends_to_messages" />
    <ListPreference android:key="motyw" android:title="Motyw" android:defaultValue="1" android:entries="@array/motyw_name" android:entryValues="@array/motyw_value" android:summary="Aby zobaczyć efekt należy ponownie uruchomić aplikację." /><CheckBoxPreference android:title="Powrót do odtwarzacza" android:key="memory_track" android:defaultValue="true" android:summary="Powrót po wybraniu piosenki z playlisty"/>
    <CheckBoxPreference android:title="Pokazuj okładki albumów" android:defaultValue="true" android:key="art_show"/>
    <CheckBoxPreference android:title="Graj po odłączeniu słuchawek" android:defaultValue="true" android:key="stop_headphone"/>
    <CheckBoxPreference android:key="wznow_call" android:defaultValue="true" android:title="Wznów po połączeniu" android:summary="Wznawianie odtwarzania po zakończeniu połączenia."/>
    

</PreferenceScreen>